# 页面结构、层级与命名（指令参考）

便于你和我用统一名称指代页面，给指令时可直接说「在 **首页** 的 xxx」「在 **炒股参谋-对话页** 里加 xxx」等。

---

## 一、顶层：4 个主页面（路由级）

应用用 `currentView` 切换，只有这 4 种状态，同一时间只显示一个：

| 页面名称（指令用） | 代码里的名字 | 说明 |
|-------------------|-------------|------|
| **首页** | `home` → `HomeView` | 入口：E智通标题、三个智能体入口（产品买手/炒股参谋/投顾猎头）、横向滑动盯盘卡片、底部导航（自选/交易 + 输入框） |
| **引导页** | `guide` → 见下方子页 | 选智能体后进入，分 3 种引导页 |
| **对话页** | `chat` → `ChatView` | 与当前智能体对话，内容随智能体不同而不同 |
| **投顾详情页** | `advisorProfile` → `AdvisorProfileView` | 从对话页点「查看详情」进入某位投顾（李华/赵金胜）的详情 |

---

## 二、引导页下的 3 个子页（按智能体区分）

`guide` 时根据 `activeAgent` 渲染不同组件，没有子路由，但可视为 3 种「子页」：

| 页面名称（指令用） | 组件名 | 对应智能体 | 说明 |
|-------------------|--------|------------|------|
| **产品买手-引导页** | `WealthGuideView` | 产品买手 | 介绍 + 进入对话 |
| **炒股参谋-引导页** | `QuantGuideView` | 炒股参谋 | 打字机介绍 + 钨矿事件 + 2 只标的 + 进入对话 |
| **投顾猎头-引导页** | `GuideView` | 投顾猎头 | 头像 + 文案 + 三个快捷问题 + 说话/打字/关闭 |

---

## 三、对话页下的「内容区」（按智能体 + 状态区分）

`ChatView` 是同一个页面，但**中间主内容**根据 `agent.id` 和内部状态切换，可理解为「对话页里的不同内容视图」：

| 内容名称（指令用） | 条件 | 说明 |
|-------------------|------|------|
| **产品买手-任务步骤** | `agent.id === 'wealth'` | 由 `FundTaskOrchestration` 按步骤展示，步骤 0~6；步骤 ≥7 时再显示 `FundCards` + 快捷话术（批量下单/加入自选/换一批） |
| **产品买手-基金卡片** | `agent.id === 'wealth'` 且 `wealthTaskStep >= 7` | `FundCards` 组件 |
| **投顾猎头-推荐列表** | `agent.id === 'advisor'` | `renderAdvisorContent()`：推荐理由标题 + 李华/赵金胜卡片（可点进投顾详情页）+ 快捷话术 |
| **炒股参谋-模型选择** | `agent.id === 'quant'` 且未选模型 | 横向滑动 4 张模型卡：趋势跟随 / 事件驱动 / 短线强势 / 高股息低波动，点「运行该模型」进入对应任务流 |
| **炒股参谋-模型任务流** | `agent.id === 'quant'` 且 `activeQuantModel` 已选 | `QuantTaskOrchestration`，按所选模型展示（如事件驱动模型） |

---

## 四、投顾详情页内部

| 区域名称（指令用） | 说明 |
|-------------------|------|
| **投顾详情-头部** | 返回、大图/渐变背景 |
| **投顾详情-信息卡** | 白底卡片：E智通·xx团队、关注/签约、标签、Tab（专业擅长 / 个人简介 / 投资理念）、文案内容 |
| **投顾详情-快捷提问** | 三个气泡：查看最新投资观点、最新推荐股票、他的组合业绩 |
| **投顾详情-底部输入** | 麦克风 + 输入框（浅色主题） |

---

## 五、首页内部（便于精确定位）

| 区域名称（指令用） | 说明 |
|-------------------|------|
| **首页-标题** | 「E 智通」 |
| **首页-三个智能体入口** | 产品买手、炒股参谋、投顾猎头（头像 + 名称） |
| **首页-盯盘卡片** | 横向滑动：产品买手盯盘提醒（富国天惠）、炒股参谋盯盘发现（航天南湖）等，带小圆点指示器 |
| **首页-底部导航** | 左侧输入框「向智能体提问...」+ 自选按钮 + 交易按钮 |

---

## 六、跳转关系（方便你说「从 A 到 B」）

```
首页
 ├─ 点「产品买手」→ 产品买手-引导页 → 进入对话 → 对话页（产品买手）
 ├─ 点「炒股参谋」→ 炒股参谋-引导页 → 进入对话 → 对话页（炒股参谋）
 └─ 点「投顾猎头」→ 投顾猎头-引导页 → 进入对话 → 对话页（投顾猎头）

对话页（投顾猎头）
 └─ 点某投顾「查看详情」→ 投顾详情页 → 返回 → 回到 对话页

对话页（炒股参谋）
 └─ 未选模型：显示 炒股参谋-模型选择；选了模型：显示 炒股参谋-模型任务流（可重置回模型选择）
```

---

给指令时可以直接用上面**加粗**的页面/区域名称，例如：

- 「在 **首页** 的盯盘卡片下面加一行文案」
- 「在 **投顾详情页** 的 Tab 里增加一个“历史业绩”」
- 「在 **炒股参谋-对话页** 的模型选择里，把第四张卡片改成 xxx」
- 「在 **产品买手-引导页** 加一个跳过按钮」

我会按这个结构理解和修改代码。
