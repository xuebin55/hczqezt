# 炒股参谋 · 页面结构与命名规范

> 本文档用于明确炒股参谋模块下所有页面的命名、触发方式和组件关系，避免在开发和指令沟通中产生混淆。

---

## 📋 页面总览

| 页面编号 | 页面名称 | 组件名称 | 触发方式 | 简要说明 |
|---------|---------|---------|---------|---------|
| Q-01 | 炒股参谋-引导页 | `QuantGuideView` | 从首页点击"炒股参谋"卡片 | 展示钨矿涨价事件介绍，点击"立即查看"进入 Q-03 |
| Q-02 | 炒股参谋-对话页（模型推荐） | `ChatView` (无激活模型) | 从引导页点击"开始对话" | 展示4个选股模型卡片，虚拟人文案："运行您喜欢的选股模型，剩下的事情交给我" |
| Q-03 | 钨矿涨价事件-选股结果页 | `WuguEventFlowView` | 从引导页(Q-01)点击"立即查看" | 展示钨矿涨价事件专属选股流程（5步）和5只精选股票 |
| Q-04 | 事件驱动模型-选股结果页 | `EventDrivenFlowView` | 从对话页(Q-02)点击"事件驱动模型"卡片的"运行该模型" | 展示多事件驱动选股流程（5步）和3只精选股票 |
| Q-05 | 趋势跟随模型-选股结果页 | `TrendFollowingFlowView` | 从对话页(Q-02)点击"趋势跟随模型"卡片的"运行该模型" | 展示趋势跟随选股流程（占位页面，待完善） |
| Q-06 | 高股息低波动模型-选股结果页 | `DividendFlowView` | 从对话页(Q-02)点击"高股息低波动模型"卡片的"运行该模型" | 展示高股息低波动选股流程（占位页面，待完善） |
| Q-07 | 短线强势模型-选股结果页 | `MomentumFlowView` | 从对话页(Q-02)点击"短线强势模型"卡片的"运行该模型" | 展示短线强势选股流程（占位页面，待完善） |

---

## 📍 详细说明

### Q-01：炒股参谋-引导页
- **组件**：`QuantGuideView`
- **路径**：首页 → 点击"炒股参谋"卡片
- **状态**：`currentView === 'guide' && activeAgent === 'quant'`
- **关键元素**：
  - 钨矿涨价事件介绍卡片
  - "立即查看"按钮 → 触发 `onStartChat('钨矿涨价事件')`，进入 Q-03
  - "开始对话"按钮 → 触发 `onStartChat()`，进入 Q-02
- **虚拟人文案**：滚动文字介绍钨矿涨价事件

---

### Q-02：炒股参谋-对话页（模型推荐）
- **组件**：`ChatView` 
- **状态条件**：
  - `currentView === 'chat'`
  - `activeAgent === 'quant'`
  - `activeQuantModel === null`（没有激活任何模型）
  - `showWuguEventFlow === false`
  - `showEventDrivenFlow === false`
- **关键元素**：
  - 4个选股模型卡片：
    1. 趋势跟随模型
    2. 事件驱动模型
    3. 高股息低波动模型
    4. 短线强势模型
  - 每个卡片都有"运行该模型"按钮
- **虚拟人文案**：`"运行您喜欢的选股模型，剩下的事情交给我"`
- **底部按钮**：`['查看历史回测结果', '分析个股']`

---

### Q-03：钨矿涨价事件-选股结果页
- **组件**：`WuguEventFlowView`
- **触发条件**：
  - 从 Q-01（引导页）点击"立即查看"
  - `initialPrompt === '钨矿涨价事件'`
  - `showWuguEventFlow === true`
- **选股流程**（5步）：
  1. 确定事件类型：行业事件 - 钨矿持续涨价
  2. 事件影响分析：上游、中游、下游影响分析
  3. 受益标的初筛：钨业务占比≥20%，相关板块筛选
  4. 基本面过滤：资产负债率、利润、现金流、PE 等条件
  5. 技术面过滤与择时：趋势看多、短线强势
- **选股结果**：5只股票（中钨高新、厦门钨业、章源钨业、翔鹭钨业、洛阳钼业）
- **关键特征**：
  - 每只股票显示：名称、代码、价格、涨跌幅、近5日涨幅、估值、钨业务占比、入选理由、买入按钮
  - 下方有3个提示词：批量买入、加入自选股、增加筛选条件
- **虚拟人文案**：
  - 运行中：`"正在运行事件驱动选股模型，为您筛选"钨矿涨价事件"重点关注投资标的。"`
  - 完成后：`"已完成筛选，为您找到 5 只重点关注投资标的。"`

---

### Q-04：事件驱动模型-选股结果页
- **组件**：`EventDrivenFlowView`
- **触发条件**：
  - 从 Q-02（对话页）点击"事件驱动模型"卡片的"运行该模型"按钮
  - `activeQuantModel === '事件驱动模型'` 或 `showEventDrivenFlow === true`
- **选股流程**（5步）：
  1. 确定事件类型与事件池：9个重要事件（3个宏观、4个行业、2个个股），事件类型用标签展示
  2. 事件影响分析：锁定15个利好板块（可展开查看详情）
  3. 受益标的初筛：事件相关度最高前100名、分析师推荐次数最高前100名
  4. 基本面排雷：非ST、无减持、低负债率、无违规、正现金流
  5. 技术面过滤与择时：趋势看多、短线强势
- **选股结果**：3只股票（中际旭创、寒武纪、比亚迪）
- **关键特征**：
  - 前两步有"修改"按钮
  - 每只股票显示：名称、代码、价格（红色）、涨跌幅（红色）、近5日涨幅、估值、关联事件数、受益事件列表、入选理由、买入按钮
  - 下方有3个提示词：批量买入、加入自选股、增加筛选条件
- **虚拟人文案**：
  - 运行中：`"正在运行事件驱动选股模型，分析近期重要事件并筛选受益标的。"`
  - 完成后：`"已完成筛选，为您找到 8 只多事件受益投资标的。"`（注：实际显示3只）

---

### Q-05 ~ Q-07：其他选股模型结果页

#### Q-05：趋势跟随模型-选股结果页
- **组件**：`TrendFollowingFlowView`
- **触发条件**：从 Q-02 点击"趋势跟随模型"卡片的"运行该模型"按钮
- **状态**：`activeQuantModel === '趋势跟随模型'`
- **当前状态**：✅ 已创建占位页面，显示页面标识
- **虚拟人文案**：
  - 运行中：`"Q-05 趋势跟随模型运行中..."`
  - 完成后：`"Q-05 趋势跟随模型选股完成"`
- **待完善**：选股流程、选股结果、股票卡片等内容

#### Q-06：高股息低波动模型-选股结果页
- **组件**：`DividendFlowView`
- **触发条件**：从 Q-02 点击"高股息低波动模型"卡片的"运行该模型"按钮
- **状态**：`activeQuantModel === '高股息低波动模型'`
- **当前状态**：✅ 已创建占位页面，显示页面标识
- **虚拟人文案**：
  - 运行中：`"Q-06 高股息低波动模型运行中..."`
  - 完成后：`"Q-06 高股息低波动模型选股完成"`
- **待完善**：选股流程、选股结果、股票卡片等内容

#### Q-07：短线强势模型-选股结果页
- **组件**：`MomentumFlowView`
- **触发条件**：从 Q-02 点击"短线强势模型"卡片的"运行该模型"按钮
- **状态**：`activeQuantModel === '短线强势模型'`
- **当前状态**：✅ 已创建占位页面，显示页面标识
- **虚拟人文案**：
  - 运行中：`"Q-07 短线强势模型运行中..."`
  - 完成后：`"Q-07 短线强势模型选股完成"`
- **待完善**：选股流程、选股结果、股票卡片等内容

---

## 🔄 页面导航关系

```
首页
  │
  ├─► Q-01 炒股参谋-引导页
  │      │
  │      ├─► "立即查看" → Q-03 钨矿涨价事件-选股结果页
  │      │
  │      └─► "开始对话" → Q-02 炒股参谋-对话页（模型推荐）
  │                           │
  │                           ├─► "趋势跟随模型" → Q-05 趋势跟随模型-选股结果页
  │                           │
  │                           ├─► "事件驱动模型" → Q-04 事件驱动模型-选股结果页
  │                           │
  │                           ├─► "高股息低波动模型" → Q-06 高股息低波动模型-选股结果页
  │                           │
  │                           └─► "短线强势模型" → Q-07 短线强势模型-选股结果页
  │
  └─► ...其他智能体
```

---

## 🎯 组件状态判断逻辑

在 `ChatView` 的 `renderQuantContent()` 中：

```typescript
const renderQuantContent = () => {
  // Q-03: 引导页进入的钨矿涨价事件选股
  if (agent.id === 'quant' && initialPrompt === '钨矿涨价事件' && showWuguEventFlow) {
    return <WuguEventFlowView ... />;
  }
  
  // Q-04: 对话页事件驱动模型选股（多事件）
  if (activeQuantModel === '事件驱动模型' || showEventDrivenFlow) {
    return <EventDrivenFlowView ... />;
  }
  
  // Q-05: 趋势跟随模型选股
  if (activeQuantModel === '趋势跟随模型') {
    return <TrendFollowingFlowView ... />;
  }
  
  // Q-06: 高股息低波动模型选股
  if (activeQuantModel === '高股息低波动模型') {
    return <DividendFlowView ... />;
  }
  
  // Q-07: 短线强势模型选股
  if (activeQuantModel === '短线强势模型') {
    return <MomentumFlowView ... />;
  }

  // Q-02: 默认显示4个模型卡片
  return <div>...4个模型卡片...</div>;
};
```

---

## 📝 给指令的建议格式

当需要修改某个页面时，请按以下格式给出指令：

### ✅ 推荐格式：
- "修改 **Q-03 钨矿涨价事件-选股结果页**，将股票数量从5只改为8只"
- "在 **Q-02 炒股参谋-对话页（模型推荐）** 中，添加一个新的模型卡片"
- "优化 **Q-04 事件驱动模型-选股结果页** 的股票卡片样式"

### ❌ 避免的格式：
- "修改炒股参谋的选股结果"（不明确是哪个结果页）
- "钨矿涨价选股页"（容易与引导页或对话页混淆）
- "事件驱动模型"（不明确是模型卡片还是选股结果页）

---

## 🔧 技术实现要点

### 关键状态变量：
- `currentView`: 'home' | 'guide' | 'chat' | 'advisorProfile'
- `activeAgent`: 'wealth' | 'quant' | 'advisor' | null
- `initialPrompt`: '钨矿涨价事件' | '事件驱动模型' | null
- `activeQuantModel`: '趋势跟随模型' | '事件驱动模型' | '高股息低波动模型' | '短线强势模型' | null
- `showWuguEventFlow`: boolean (专用于钨矿涨价事件流程)
- `showEventDrivenFlow`: boolean (专用于多事件驱动流程)
- `wuguFlowCompleted`: boolean (钨矿涨价事件是否完成)
- `eventDrivenCompleted`: boolean (多事件驱动是否完成)
- `trendFollowingCompleted`: boolean (趋势跟随模型是否完成)
- `dividendCompleted`: boolean (高股息低波动模型是否完成)
- `momentumCompleted`: boolean (短线强势模型是否完成)

### 组件文件位置：
所有组件均在 `/Users/yiliu/Desktop/财富管理-agent-0314/src/App.tsx` 中定义

---

## 📅 更新日志

- **2026-03-14**：初始版本，记录 Q-01 至 Q-04 页面
- **2026-03-14**：✅ 新增 Q-05、Q-06、Q-07 三个选股模型占位页面，各页面已可独立访问和区分

---

## 💡 未来扩展建议

1. 每个选股模型应该有独立的组件（如 `TrendFollowingFlowView`、`DividendFlowView` 等）
2. 建议将选股结果页组件独立到单独的文件中（如 `components/quant/` 目录）
3. 可以考虑使用路由管理代替状态管理，使页面切换更清晰
4. 每个选股结果页可以有统一的 Props 接口，便于维护
